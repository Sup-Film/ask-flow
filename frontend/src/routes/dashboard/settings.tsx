export default function DashboardSettings() {
  return (
    <div>
      <h2 className="text-lg font-semibold">Settings (Nested Sibling)</h2>
      <p>This is a sibling route inside the dashboard layout.</p>
    </div>
  );
}
