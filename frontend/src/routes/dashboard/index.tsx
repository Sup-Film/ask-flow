export default function DashboardIndex() {
  return (
    <div>
      <h2 className="text-lg font-semibold">
        Dashboard Overview (Index Route)
      </h2>
      <p>This is the default page shown at /dashboard.</p>
    </div>
  );
}
